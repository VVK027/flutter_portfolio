# Vivek Kumar — Flutter Web Portfolio

## Run locally
1. Ensure Flutter 3.22+ is installed with web enabled (`flutter config --enable-web`).
2. From this folder, run: `flutter pub get`.
3. Start in Chrome: `flutter run -d chrome`.

## Build for web
- `flutter build web` (output in `build/web`).
